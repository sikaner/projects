# NEVER hardcode secrets in source control. Inject via --extra-vars or Ansible Vault.
OPENAI_API_KEY={{ openai_api_key }}
# Optional metadata (not required by the SDK but kept per your request)
OPENAI_PROJECT={{ openai_project | default('My First Project (fast-gate-439411-i4)') }}


# API settings
APP_PORT={{ app_port | default('8000') }}
MODEL_DIR={{ model_dir | default('/app/model') }}
